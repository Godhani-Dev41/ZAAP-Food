import { ExtraIngredientStringPipe } from './extra-ingredient-string.pipe';

describe('ExtraIngredientStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraIngredientStringPipe();
    expect(pipe).toBeTruthy();
  });
});
